import { PureComponent, ReactNode } from "react";
import { h } from "../..";

export default class MyProvider extends PureComponent {
  render(): ReactNode {
    return (
      <div>
        1
      </div>
    );
  }
}